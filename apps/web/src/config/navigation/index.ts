/**
 * Tenant sidebar navigation — domain model, items, and route matching.
 * Prefer importing from here or from `config/nav` + `config/navMatch`.
 *
 * See `docs/NAVIGATION.md` for structure, deprecated APIs, and contributor rules.
 */
export * from '../nav';
export * from '../navMatch';
